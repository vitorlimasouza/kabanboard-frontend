import { Button, Input, Painel, Container } from "../styles";

export const Singup = () => {
    return(
        <Container>
            <Painel>
                <Input/>
                <Input/>
                <Button>Enviar</Button>
            </Painel>
        </Container>
    )
} 