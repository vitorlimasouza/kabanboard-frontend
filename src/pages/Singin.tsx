import { Button, Container, Input, Painel } from "../styles";

export const Singin = () => {
    return(
        <Container>
            <Painel>
                <Input/>
                <Input/>
                <Button>Enviar</Button>
            </Painel>
        </Container>
    )
} 